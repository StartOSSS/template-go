server:
  http_listen_port: 3200
distributor:
  receivers:
    otlp:
      protocols:
        http: {}
ingester:
  max_block_bytes: 1000000
storage:
  trace:
    backend: local
    local:
      path: /tmp/tempo
