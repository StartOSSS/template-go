apiVersion: v1
kind: Secret
metadata:
  name: {{ include "postgres.fullname" . }}
type: Opaque
stringData:
  POSTGRES_USER: {{ .Values.credentials.username | quote }}
  POSTGRES_PASSWORD: {{ .Values.credentials.password | quote }}
  POSTGRES_DB: {{ .Values.credentials.database | quote }}
